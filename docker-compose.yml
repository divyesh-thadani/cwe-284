services:
  cwe284-lab:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cwe284-lab
    environment:
      LAB_MODE: ${LAB_MODE:-vuln}
    ports:
      - "${HOST_PORT:-5000}:5000"
    restart: unless-stopped
