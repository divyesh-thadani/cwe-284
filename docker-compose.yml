services:
  cwe284-lab:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cwe284-lab
    environment:
      LAB_MODE: ${LAB_MODE:-vuln}
    ports:
      - "3000:3000"
    restart: unless-stopped
